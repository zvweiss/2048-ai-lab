/**
 * Trainer CLI (seed).
 *
 * In Track A, training happens locally and produces artifacts under /artifacts (gitignored).
 * Later we will add:
 * - tfjs-node model creation
 * - self-play loop
 * - replay buffer
 * - evaluation harness
 * - artifact export into apps/ui/src/assets/models/v###/
 */

console.log("Trainer CLI seed. (TODO: implement LAB training loop.)");
process.exit(0);
