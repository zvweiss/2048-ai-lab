// import type { Direction, GameState } from "@zvi/ai-2048-core";

// export type AgentId = "random" | "expectimax";

// export interface Agent {
//   id: AgentId;
//   selectMove(state: GameState): Direction | null; // null means no legal moves
// }